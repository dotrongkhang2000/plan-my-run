{
  "name": "db",
  "version": "1.0.0",
  "description": "Prisma Client",
  "main": "index.ts",
  "scripts": {
    "dev": "prisma studio",
    "migrate": "prisma migrate dev",
    "generate": "prisma generate"
  }
}
